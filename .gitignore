node_modules
dist
dist-ssr
coverage
playwright-report
test-results
*.local
*.tsbuildinfo

# Generated by an older tsconfig.node.json that had emit enabled.
# Kept here so a stale local copy never gets committed again.
vite.config.js

# Editor
.vscode/*
!.vscode/extensions.json
.DS_Store

# Signing material - never commit
*.jks
*.keystore

# Hosting
.vercel
.netlify

# Secrets. The negation must stay LAST: in .gitignore the final matching
# pattern wins, so putting `.env*` after `!.env.example` would re-ignore it.
.env
.env.*
!.env.example
